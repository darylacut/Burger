<li>
	{{name}}

	<button class="eat" data-id="{{id}}" data-neweat="{{devoured}}">
		{{#if devoured}}Take Out!{{else}}Dine In!{{/if}}
	</button>

	{{!-- <button class="delete-burger" data-id="{{this.id}}">Cancel Order!</button> --}}
</li>
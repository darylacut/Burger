<div class="card text-center">
  <div class="card-header">
    Featured
  </div>


{{!-- ===================     body      ===================== --}}


  <div class="card-body">
    <h1 class="HdrTxt">Welcome to Burger Bites!</h1>
<h4 class="SubHdrTxt">Please place your order</h4>

<h2>What's your favorite burger?</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burg">Burger Name:</label>
    <input type="text" id="burg" name="name">
  </div>

  <button type="submit" id="newBurgBtn">Order Burger</button>
</form>

<h2>Burgers waiting to be devoured!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>


<h2>These burgers are in your stomach!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>


  </div>


  <div class="card-footer text-muted">
    2 days ago
  </div>
</div>